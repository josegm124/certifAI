PORT=3001
LOG_LEVEL=info
NODE_ENV=development

# Database reset mode (development only)
# true = Auto-reset DB + uploads on every start (like Spring's create-drop)
# false = Keep data between restarts
RESET_DB_ON_START=true
