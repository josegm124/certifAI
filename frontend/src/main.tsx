import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";

// v2 stored the complete assessment and personal details in the browser.
// v3 keeps only unsynchronised domain answers and recovers the rest via login.
localStorage.removeItem("certifai-v2");

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
